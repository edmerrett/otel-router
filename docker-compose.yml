# Demo harness: the router fans out to two differently-shaped destinations.
# webhook-siem is a plain HTTP echo server standing in for a webhook-style
# SIEM feed (logs only, JSON, access-key header); sink-app is a real OTLP
# collector standing in for an observability backend (all signals). Run
# `docker compose up` and watch both receive data, while gen-noauth (no
# bearer token) is rejected. Run demo/test.sh for the asserted version.

services:
  router:
    build: .
    environment:
      INBOUND_TOKEN: demo-inbound-token
      SIEM_ENDPOINT: http://webhook-siem:8080/v1/ingest
      SIEM_AUTH: demo-webhook-key
      APP_ENDPOINT: http://sink-app:4318
      APP_AUTH: Bearer demo-app-token
    ports:
      - "4317:4317"
      - "4318:4318"
    depends_on:
      - webhook-siem
      - sink-app

  # Logs every request it receives (method, path, headers, body) to stdout.
  webhook-siem:
    image: mendhak/http-https-echo:37

  sink-app:
    image: otel/opentelemetry-collector:0.156.0
    command: ["--config=/etc/otelcol/sink.yaml"]
    volumes:
      - ./demo/sink.yaml:/etc/otelcol/sink.yaml:ro

  gen-traces:
    image: ghcr.io/open-telemetry/opentelemetry-collector-contrib/telemetrygen:latest
    command: ["traces", "--otlp-endpoint", "router:4317", "--otlp-insecure",
              "--otlp-header", 'Authorization="Bearer demo-inbound-token"',
              "--duration", "5s"]
    depends_on:
      - router

  gen-metrics:
    image: ghcr.io/open-telemetry/opentelemetry-collector-contrib/telemetrygen:latest
    command: ["metrics", "--otlp-endpoint", "router:4317", "--otlp-insecure",
              "--otlp-header", 'Authorization="Bearer demo-inbound-token"',
              "--duration", "5s"]
    depends_on:
      - router

  gen-logs:
    image: ghcr.io/open-telemetry/opentelemetry-collector-contrib/telemetrygen:latest
    command: ["logs", "--otlp-endpoint", "router:4317", "--otlp-insecure",
              "--otlp-header", 'Authorization="Bearer demo-inbound-token"',
              "--duration", "5s"]
    depends_on:
      - router

  # Deliberately sends without a token; the router must reject it.
  gen-noauth:
    image: ghcr.io/open-telemetry/opentelemetry-collector-contrib/telemetrygen:latest
    command: ["traces", "--otlp-endpoint", "router:4317", "--otlp-insecure",
              "--duration", "2s"]
    depends_on:
      - router
